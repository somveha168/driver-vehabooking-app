/// Named routes. Keep in sync with [AppPages].
abstract class Routes {
  Routes._();

  static const login = '/login';
  static const home = '/home';
  static const bookingDetail = '/booking-detail';
  static const documents = '/documents';
}
